<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title || "Delete Vehicle" %></title>
</head>
<body>
  <h1><%= title || "Delete New Vehicle" %></h1>
  <p><strong>Confirm Deletion - The delete is permanent.</strong></p>
  <form id="deleteForm" action="/inv/delete" method="post">

    <label for="inv_make">Make</label>
    <input type="text" name="inv_make" id="inv_make" required value="<%= inv_make %>" readonly>

    <label for="inv_model">Model</label>
    <input type="text" name="inv_model" id="inv_model" required value="<%= inv_model %>" readonly>

    <label for="inv_price">Price</label>
    <input type="number" name="inv_price" id="inv_price" required min="0" value="<%= inv_price %>" readonly>

    <label for="inv_year">Year</label>
    <input type="number" name="inv_year" id="inv_year" required min="1900" max="2099" value="<%= inv_year %>" readonly>

    <input type="hidden" name="inv_id" value="<%= inv_id %>">

    <button type="submit" name="submit">Delete Vehicle</button>
  </form>
</body>
</html>
